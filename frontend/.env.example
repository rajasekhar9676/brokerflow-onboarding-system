# BrokerFlow Frontend - copy to .env for local overrides

# Local dev: use /api (Vite proxy to backend). Production build uses .env.production.
VITE_API_URL=/api

# Production (or set in hosting env): uncomment and use your backend URL
# VITE_API_URL=https://brokerflow-onboarding-system.onrender.com/api
